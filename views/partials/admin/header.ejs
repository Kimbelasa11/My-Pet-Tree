<header class="admin-topbar">
  <div class="admin-topbar-left">
    <h1><%= typeof title !== 'undefined' ? title.split(' — ')[0] : 'Dashboard' %></h1>
  </div>
  <div class="topbar-right">
    <span class="topbar-user">
      <span class="topbar-avatar"><%= (typeof user !== 'undefined' ? user.username : 'A').charAt(0).toUpperCase() %></span>
      <strong><%= typeof user !== 'undefined' ? user.username : 'admin' %></strong>
    </span>
  </div>
</header>